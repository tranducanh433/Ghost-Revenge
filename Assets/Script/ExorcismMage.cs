using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExorcismMage : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        StartFunc();
    }

    // Update is called once per frame
    void Update()
    {
        UpdateFunc();
    }
}
